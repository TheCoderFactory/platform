<nav class="navbar navbar-default navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container">
       <div class="row">
        <div class="col-md-12">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Menu</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to root_path, :class => 'navbar-brand' do %>
       Cerebr<span style="font-size: 24px;"><i class="icon-plus-sign icon-spin"></i> <em>beta</em></span>
    <% end %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse pull-right">
    <ul class="nav navbar-nav">
      <% if user_signed_in? %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-fighter-jet"></i> Quick links <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <% if @quicklinks.count > 0 %>
            <% @quicklinks.each do |quicklink| %>
              <li>
                <%= link_to quicklink.url do %><i class="icon-<%= quicklink.icon %>"></i> <%= quicklink.name %><% end %>
              </li>
            <% end %>
          <% end %>
          <% if @important_links.count > 0 %>
            <li class="divider"></li>
            <% @important_links.each do |important_link| %>
              <li>
                <%= link_to important_link.url do %><i class="icon-<%= important_link.icon %>"></i> <%= important_link.name %><% end %>
              </li>
            <% end %>
          <% end %>
          
          <li class="divider"></li>
          <% @qlinks.each do |link| %>
            <li><%= link_to link.title, link.url, target: "_blank" %></li>
          <% end %>
          <!-- <li><a href="#">Action</a></li>
          <li><a href="#">Another action</a></li>
          <li><a href="#">Something else here</a></li>
          <li><a href="#">Separated link</a></li> -->
        </ul>
      </li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user"></i> <%= current_user.name %> <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to edit_user_registration_path do %><i class="icon-edit"></i> Edit account<% end %></li>
          <li><%= link_to edit_user_profile_path(current_user.user_profile) do %><i class="icon-edit"></i> Edit profile<% end %></li>
          <li><%= link_to destroy_user_session_path, :method=>'delete' do %><i class="icon-signout"></i> Sign out<% end %></li>
        </ul>
      </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to 'Admin', admin_path %>
      </li>
    <% end %>
    
  <% else %>
    <li>
    <%= link_to 'Login', new_user_session_path %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    
    
      
  <% else %>
    <li>
    <%= link_to 'Sign up', new_user_registration_path %>
    </li>
  <% end %>
    </ul>
    <!-- <form class="navbar-form navbar-left" role="search">
      <div class="form-group">
        <input type="text" class="form-control" placeholder="Search">
      </div>
      <button type="submit" class="btn btn-default">Submit</button>
    </form> -->
   <!--  <ul class="nav navbar-nav navbar-right">
      
    </ul>  -->
  </div><!-- /.navbar-collapse -->
</div>
</div>
</div>
</nav>


