<%= simple_form_for(@quiz) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :week %>
    <%= f.input :title %>
    <%= f.input :intro %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
